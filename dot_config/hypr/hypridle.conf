listener {
  timeout = 600 # in seconds
  on-timeout = hyprctl dispatch dpms off
  on-resume = sh -lc 'hyprctl dispatch dpms on; sleep 0.5; $HOME/.config/hypr/scripts/clamshell-mode.sh --reason resume --settle-seconds 6 --retry-interval 0.5; $HOME/.config/hypr/scripts/restore-wallpaper.sh'
}
